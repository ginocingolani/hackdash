{
  "name": "hackdash",
  "private": true,
  "version": "2.0.0-alpha.0",
  "description": "HackDash — organize hackathon ideas into a dashboard. Modern rewrite.",
  "license": "MIT",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "pnpm --filter @hackdash/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
