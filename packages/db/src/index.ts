// @hackdash/db — shared data layer for the HackDash rewrite.
// Schemas are ported from the legacy Mongoose models so existing
// hackdash.org data remains readable without a destructive migration.
// Populated as the legacy model map lands in docs/legacy/.

export {};
